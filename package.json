{
  "name": "reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local pi extension for reading and exporting assistant responses.",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.5.0",
    "typescript": "^5.9.3"
  }
}
